# Verifies code quality and build integrity on every PR into main.
#
# Four jobs:
#   frontend-check   — lint + unit tests + prod image build (ubuntu-latest)
#   launcher-check   — unit tests + the tsc/vite build tauri runs (ubuntu-latest)
#   backend-smoke    — ruff + import smoke test + ops script tests
#   backend-full-test — full pytest suite with Mongo replica-set service
#
# Backend Docker images are NOT built on PRs: they compile bwa-mem2, Clair3,
# and compleasm from source and are expensive. The backend is instead
# smoke-tested here (ruff + import + ops tests). The full pytest suite runs
# as a separate job with a Mongo replica-set container.

name: Build check

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - '**/*.mdx'
      - 'LICENSE'
  workflow_dispatch:
    inputs:
      skip_frontend:
        description: 'Skip the frontend check job'
        type: boolean
        default: false
      skip_backend:
        description: 'Skip the backend smoke test job'
        type: boolean
        default: false
      skip_launcher:
        description: 'Skip the launcher check job'
        type: boolean
        default: false
      skip_backend_full:
        description: 'Skip the full backend test suite job'
        type: boolean
        default: false

# A new push to the same PR supersedes the in-flight check for the old one.
concurrency:
  group: build-check-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend-check:
    name: Frontend (lint + test + build)
    if: ${{ !inputs.skip_frontend }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Lint (tsc --noEmit)
        working-directory: frontend
        run: npm run lint

      - name: Test (vitest)
        working-directory: frontend
        run: npm run test

      # No push, no registry login: this only needs to prove the image
      # builds. `npm run build` runs inside the `build` stage that `prod`
      # depends on, so a Vite/esbuild syntax error fails this step exactly
      # the way it fails `docker compose up -d --build` locally.
      # setup-buildx-action is required for the GHA cache backend (type=gha)
      # — without it, build-push-action falls back to the default `docker`
      # driver which can't export the cache.
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build prod image
        uses: docker/build-push-action@v7
        with:
          context: ./frontend
          target: prod
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

  launcher-check:
    name: Launcher (test + build)
    if: ${{ !inputs.skip_launcher }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: launcher/package-lock.json

      - name: Install dependencies
        working-directory: launcher
        run: npm ci

      - name: Test (vitest)
        working-directory: launcher
        run: npm run test

      # This runs `tsc -b && vite build` -- the same command tauri invokes as
      # its beforeBuildCommand, and deliberately not `npm run lint`
      # (`tsc --noEmit`), which is a different compiler mode. The Rust side is
      # not built here: it is slow, needs platform toolchains, and the failure
      # this job exists to catch happens before cargo is ever reached.
      #
      # v0.5.0's macOS and Linux bundles both failed on a test file missing its
      # `import { describe, expect, it } from "vitest"`. Vitest injects those
      # globals at runtime so `npm run test` passed, but tsconfig has
      # `include: ["src"]` and no vitest global types, so `tsc` failed -- and
      # nothing in the PR path compiled the launcher at all.
      - name: Build (tsc -b && vite build)
        working-directory: launcher
        run: npm run build

  backend-smoke:
    name: Backend smoke (ruff + import + ops tests)
    if: ${{ !inputs.skip_backend }}
    runs-on: ubuntu-latest
    # Default (no timeout-minutes) is GitHub's 360-minute hard cap. A stalled
    # apt-get mirror in "Install system build dependencies" has hung this job
    # for over an hour with nothing to fail it (#577) — 15 min gives a real
    # margin over the ~2-3 min this job normally takes end to end.
    #
    # This is the outer backstop, not the first line of defence: both apt-get
    # steps below bound their own fetches (see the note on the first one), so
    # a dead mirror should fail in about a minute rather than burning the
    # full 15. If this timeout is what ends a run, the apt options did not
    # catch it and the stall is something else.
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: backend/pyproject.toml

      # pysam (C extension against htslib) and cryptography (Rust) need
      # toolchain headers that aren't in the base ubuntu-latest image.
      #
      # The -o options bound how long a bad mirror can stall this step
      # (#577). ubuntu-latest resolves apt sources through a mirrorlist
      # (file:/etc/apt/apt-mirrors.txt) that lists Azure's internal
      # azure.archive.ubuntu.com first and public archive.ubuntu.com as
      # fallback, so a fetch can fail two different ways:
      #
      #   - Connect never completes. apt gives up per its own timeout and
      #     falls through to the next mirror. Visible as `Ign:` lines.
      #   - Connect succeeds, then the transfer stalls mid-stream. This is
      #     the one that hurt: the socket stays open, so nothing declares it
      #     dead, and apt waits indefinitely. On PR #609 the last log line
      #     was a `Get:` for a 126 kB InRelease that never arrived, and the
      #     job sat there until the 15-minute job timeout killed it.
      #
      # Timeout is what bounds the second case: it is an inactivity deadline
      # on the transfer, not just a connect deadline, so it fires on a mirror
      # that accepts the connection and then goes quiet. Retries covers a
      # mirror that is briefly down rather than slow. Both http and https are
      # set because the mirrorlist mixes schemes — the Azure entry is http,
      # the public fallback https — and each scheme reads its own key.
      #
      # Retries multiplies the timeout rather than replacing it: the budget
      # per source is (1 + Retries) x Timeout, so 15s here means a dead
      # source costs ~60s, not 15s. Measured against a server that accepts
      # the connection and then stops sending, elapsed tracks that formula
      # closely — Timeout=5 -> 27s, 15 -> 67s, 30 -> 127s. 15s is chosen to
      # stay well inside the 15-minute job timeout even if several sources
      # fail in sequence, while leaving enough headroom that a merely slow
      # (not dead) mirror still succeeds.
      - name: Install system build dependencies
        run: |
          sudo apt-get -o Acquire::Retries=3 \
                       -o Acquire::http::Timeout=15 \
                       -o Acquire::https::Timeout=15 \
                       update
          sudo apt-get install -y build-essential zlib1g-dev

      # Ruff is a standalone binary (no dependencies of its own), so install
      # it first — this gives fast lint feedback before the heavier backend
      # install step runs.
      #
      # --config is explicit rather than relied on for auto-discovery:
      # backend/pyproject.toml only auto-applies to paths under backend/, so
      # ops/ and e2e/ would otherwise silently fall back to ruff's own
      # built-in defaults -- a different, smaller rule set (see #466).
      - name: Lint with ruff
        run: |
          pip install ruff
          ruff check --config backend/pyproject.toml backend/app backend/tests ops e2e

      - name: Install backend dev dependencies
        run: pip install -e 'backend/.[dev]'

      # Exercises the full import chain (every router, service, and the MCP
      # server module) without connecting to Mongo or Redis — create_app()
      # only wires up routes; lifespan (which connects to DBs) runs later.
      - name: Import smoke test
        run: python -c "import app.main; print('backend imports OK')"

      # The ops tests are self-contained (subprocess + tmp_path fixtures,
      # no backend imports) and validate the release-version guard and
      # version-bump scripts that release.yml and release-launcher.yml
      # depend on.
      - name: Ops script tests
        run: pytest ops/tests/ -v

      # Static analysis of the shell scripts, added in #492. Worth stating
      # what this does NOT do: it does not catch the locale-dependent
      # unbraced-expansion bug that issue was filed about. shellcheck reports
      # `log "$dir…"` clean, because it is valid syntax whose meaning depends
      # on the runtime LC_CTYPE. The tests in ops/tests/test_backup_restore.py
      # are what cover that. This step guards a different class -- quoting,
      # globbing, unused variables -- and the tree was already clean when it
      # was added, so a failure here is a regression, not a backlog.
      - name: Lint shell scripts with shellcheck
        run: |
          # Same mirror-stall exposure as the build-dependencies step above;
          # see the note there for why these options are set (#577).
          sudo apt-get -o Acquire::Retries=3 \
                       -o Acquire::http::Timeout=15 \
                       -o Acquire::https::Timeout=15 \
                       update
          sudo apt-get install -y shellcheck
          # ops/hooks/commit-msg is named explicitly: it is a bash script
          # with no extension, so a *.sh glob skips it silently.
          shellcheck ops/*.sh ops/hooks/*.sh ops/hooks/commit-msg

  backend-full-test:
    name: Backend tests (full pytest suite)
    if: ${{ !inputs.skip_backend_full }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: backend/pyproject.toml

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential zlib1g-dev libhts-dev curl

      - name: Install pipeline tools
        # The suite normally runs inside the backend image, which ships these.
        # Launch-path tests probe them for real (shutil.which + --version), so
        # a bare runner fails with "minimap2 is not available" / "flye is not
        # installed" rather than skipping. tabix provides bgzip, which
        # storage/test_compress needs to contrast against the stdlib fallback.
        run: |
          sudo apt-get install -y --no-install-recommends \
            minimap2 samtools tabix flye fastp bcftools bowtie2 hisat2 \
            sra-toolkit
          # The NCBI datasets CLI is not packaged; fetch the static binary the
          # same way backend/Dockerfile does. Route tests hit its probe before
          # their own assertions (a missing tool 500s where they expect 404).
          sudo curl -fsSL \
            "https://ftp.ncbi.nlm.nih.gov/pub/datasets/command-line/v2/linux-amd64/datasets" \
            -o /usr/local/bin/datasets
          sudo chmod +x /usr/local/bin/datasets
          datasets --version
          # align_runner builds "/bin/sh -o pipefail -c ..." pipelines and two
          # tests execute them for real. The runner's dash rejects -o pipefail
          # (exit 2 even for `true | true`); the backend image's shell accepts
          # it, so point sh at bash to match.
          sudo ln -sf bash /bin/sh
          /bin/sh -o pipefail -c 'true | true'
          # bwa-mem2 is not packaged for Debian/Ubuntu; use the upstream
          # prebuilt x86-64 bundle, installed the same way backend/Dockerfile
          # does: a wrapper, not a symlink -- the `bwa-mem2` entrypoint is a
          # dispatcher that looks for its SIMD variants next to argv[0], so a
          # symlink in /usr/local/bin sends it hunting for variants that are
          # not there.
          curl -fsSL https://github.com/bwa-mem2/bwa-mem2/releases/download/v2.2.1/bwa-mem2-2.2.1_x64-linux.tar.bz2 \
            | sudo tar -xj -C /opt
          printf '#!/bin/sh\nexec "/opt/bwa-mem2-2.2.1_x64-linux/bwa-mem2" "$@"\n' \
            | sudo tee /usr/local/bin/bwa-mem2 >/dev/null
          sudo chmod +x /usr/local/bin/bwa-mem2
          # No --version flag: it prints usage (with the version) and exits
          # non-zero, so tolerate the exit code and just prove it dispatches.
          bwa-mem2 version || true

      - name: Install backend dev dependencies
        run: pip install -e 'backend/.[dev]'

      - name: Create data directory
        # Mirrors what initialize_home() lays out at API startup. The
        # .biopipe/VERSION sentinel is what require_home() checks before every
        # write path; without it the storage layer refuses to run and dozens
        # of ingest/share/results tests fail with StorageUnavailableError.
        run: |
          sudo mkdir -p /data/.biopipe /data/objects /data/staging /data/tmp
          echo "biopipe-home-v1" | sudo tee /data/.biopipe/VERSION
          sudo chmod -R 777 /data

      - name: Start MongoDB with replica set
        run: |
          docker run -d --name mongo \
            -p 27017:27017 \
            mongo:7 --replSet rs0 --bind_ip_all
          # Wait for MongoDB to accept connections
          for i in $(seq 1 30); do
            if docker exec mongo mongosh --quiet --eval 'db.adminCommand("ping")' >/dev/null 2>&1; then
              echo "MongoDB is ready"
              break
            fi
            sleep 1
          done

      - name: Initialize replica set
        run: |
          # Wait for the primary to accept connections, then initiate.
          for i in $(seq 1 30); do
            if docker exec mongo mongosh --quiet --eval 'db.adminCommand("ping")' >/dev/null 2>&1; then
              echo "MongoDB is ready"
              break
            fi
            sleep 1
          done
          # Initialize the replica set
          docker exec mongo mongosh --quiet \
            --eval 'rs.initiate({_id: "rs0", members: [{_id: 0, host: "localhost:27017"}]})'
          sleep 3

      - name: Run full test suite
        run: pytest backend/tests/ -q --tb=no
        env:
          MONGO_URL: "mongodb://localhost:27017/biopipe_test?replicaSet=rs0&directConnection=true"
