# Verifies code quality and build integrity on every PR into main.
#
# Two jobs (plus a commented-out full-suite template):
#   frontend-check — lint + unit tests + prod image build (ubuntu-latest)
#   backend-smoke  — ruff + import smoke test + ops script tests
#
# Backend Docker images are NOT built on PRs: they compile bwa-mem2, Clair3,
# and compleasm from source and are expensive. The backend is instead
# smoke-tested here (ruff + import + ops tests). The full pytest suite is
# deferred to the commented-out backend-full-test job, which needs a Mongo
# replica-set service — available on ubuntu-latest as a service container.

name: Build check

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - '**/*.mdx'
      - 'LICENSE'
  workflow_dispatch:
    inputs:
      skip_frontend:
        description: 'Skip the frontend check job'
        type: boolean
        default: false
      skip_backend:
        description: 'Skip the backend smoke test job'
        type: boolean
        default: false

# A new push to the same PR supersedes the in-flight check for the old one.
concurrency:
  group: build-check-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend-check:
    name: Frontend (lint + test + build)
    if: ${{ !inputs.skip_frontend }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Lint (tsc --noEmit)
        working-directory: frontend
        run: npm run lint

      - name: Test (vitest)
        working-directory: frontend
        run: npm run test

      # No push, no registry login: this only needs to prove the image
      # builds. `npm run build` runs inside the `build` stage that `prod`
      # depends on, so a Vite/esbuild syntax error fails this step exactly
      # the way it fails `docker compose up -d --build` locally.
      # setup-buildx-action is required for the GHA cache backend (type=gha)
      # — without it, build-push-action falls back to the default `docker`
      # driver which can't export the cache.
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build prod image
        uses: docker/build-push-action@v7
        with:
          context: ./frontend
          target: prod
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

  backend-smoke:
    name: Backend smoke (ruff + import + ops tests)
    if: ${{ !inputs.skip_backend }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: backend/pyproject.toml

      # pysam (C extension against htslib) and cryptography (Rust) need
      # toolchain headers that aren't in the base ubuntu-latest image.
      - name: Install system build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential zlib1g-dev

      # Ruff is a standalone binary (no dependencies of its own), so install
      # it first — this gives fast lint feedback before the heavier backend
      # install step runs.
      - name: Lint with ruff
        run: |
          pip install ruff
          ruff check backend/app

      - name: Install backend dev dependencies
        run: pip install -e 'backend/.[dev]'

      # Exercises the full import chain (every router, service, and the MCP
      # server module) without connecting to Mongo or Redis — create_app()
      # only wires up routes; lifespan (which connects to DBs) runs later.
      - name: Import smoke test
        run: python -c "import app.main; print('backend imports OK')"

      # The ops tests are self-contained (subprocess + tmp_path fixtures,
      # no backend imports) and validate the release-version guard and
      # version-bump scripts that release.yml and release-launcher.yml
      # depend on.
      - name: Ops script tests
        run: pytest ops/tests/ -v

  # ------------------------------------------------------------------
  # FUTURE: Full backend test suite with Mongo replica-set service.
  #
  # The smoke test above covers ruff + imports; this job adds the
  # full pytest suite (312 test files) that need a real Mongo replica set —
  # the conftest hardcodes database "biopipe_test" and the
  # beanie_models fixture (scope="module") drops all collections on entry.
  #
  # Run order: install deps → init replica set → pytest.
  # ------------------------------------------------------------------
  #
  # backend-full-test:
  #   name: Backend tests (full pytest suite)
  #   runs-on: ubuntu-latest
  #   services:
  #     # A single-node replica set is required for MongoDB transactions
  #     # (used by blob refcount updates, share cascades, etc.). The
  #     # conftest hardcodes mongo_url pointing at "rs0".
  #     mongo:
  #       image: mongo:7
  #       ports: ['27017:27017']
  #       env:
  #         MONGO_INITDB_ROOT_USERNAME: root
  #         MONGO_INITDB_ROOT_PASSWORD: example
  #       options: --replSet rs0 --bind_ip_all
  #   steps:
  #     - uses: actions/checkout@v7
  #
  #     - uses: actions/setup-python@v7
  #       with:
  #         python-version: '3.12'
  #         cache: 'pip'
  #         cache-dependency-path: backend/pyproject.toml
  #
  #     - name: Install build dependencies
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y build-essential zlib1g-dev libhts-dev curl
  #
  #     - name: Install backend dev dependencies
  #       run: pip install -e 'backend/.[dev]'
  #
  #     - name: Install mongosh
  #       # No Mongo shell on the base image; needed to init the replica set.
  #       run: |
  #         curl -fsSL https://www.mongodb.com/serversh/scripts/mongodb-shell.sh | bash
  #
  #     - name: Initialize replica set
  #       run: |
  #         # Wait for the primary to accept connections, then initiate.
  #         until mongosh "mongodb://root:example@localhost:27017/admin?directConnection=true" \
  #           --quiet --eval 'db.adminCommand("ping")' >/dev/null 2>&1; do
  #           sleep 1
  #         done
  #         mongosh "mongodb://root:example@localhost:27017/admin?directConnection=true" \
  #           --quiet \
  #           --eval 'rs.initiate({_id: "rs0", members: [{_id: 0, host: "localhost:27017"}]})'
  #         sleep 3
  #
  #     - name: Run full test suite
  #       run: pytest backend/tests/ -v
  #       env:
  #         MONGO_URL: "mongodb://root:example@localhost:27017/biopipe_test?replicaSet=rs0&directConnection=true"
  #         REDIS_URL: "redis://localhost:6379/0"
