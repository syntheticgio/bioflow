# BioFlow E2E Test Harness

A Hermes desktop plugin that runs end-to-end tests of BioFlow through its MCP server and HTTP API. Install dependencies with `./e2e/install.sh`, run tests from the dashboard page, and configure the harness at `~/.hermes/plugins/bioflow-e2e/data/config.json`.
