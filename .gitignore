.env
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
node_modules/
dist/
.DS_Store
*.log
!backend/tests/fixtures/tool_logs/*.log
.idea/
.vscode/
tsconfig.tsbuildinfo
backend/.venv/

# Superpowers brainstorming scratch (mockups, session state)
.superpowers/
skills-lock.json

# Local trend history for ops/test-timing.sh
ops/.test-timing-history.jsonl
.obsidian/
.mcp.json

# Isolated feature worktrees
.worktrees/
.pi-subagents/

# Backups written by ops/backup.sh. Timestamped directories, never pruned.
backups/

# Per-machine build setup: signing identities, API key IDs, local file paths.
# Never contains the private key itself (that lives outside the repo), but
# does carry Key ID / Issuer ID, which are still not something to publish.
launcher/BUILDING.local.md
launcher/node_modules
